import 'package:flutter/material.dart';

enum ActionType {
  ADD,
  EDIT
}

enum BannerAdType {
  SMART,
  ADAPTIVE,
}

enum PurchaseMode {
  DONATE,
  DELETE_AD,
  SUBSCRIPTION
}

enum ScreenType {
  INPUT_SCREEN,
  CALENDAR_SCREEN,
}

enum StartingDayOfWeek {
  MONDAY,
  SUNDAY
}

enum ScaleType {
  WEIGHT,
  HEIGHT
}


enum Position {
  LEFT,
  CENTER,
  RIGHT
}

enum InputType {
  NAME,
  INTAKE,
}

enum DataType {
  STRING,
  INTEGER,
  DOUBLE
}

enum LayoutType {
  VERTICAL,
  HORIZONTAL,
}
